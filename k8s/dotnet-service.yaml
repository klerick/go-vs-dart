apiVersion: v1
kind: Service
metadata:
  name: dotnet-service
  namespace: bench
spec:
  selector:
    app: dotnet-service
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet-service
  namespace: bench
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dotnet-service
  template:
    metadata:
      labels:
        app: dotnet-service
    spec:
      nodeSelector:
        kubernetes.io/hostname: your-app-node
      containers:
        - name: dotnet-service
          image: your-registry.example.com/library/dotnet-bench:latest
          env:
            - name: HTTP_PORT
              value: "8080"
            - name: POSTGRES_URL
              value: "postgres://bench:bench@postgres:5432/bench?sslmode=disable"
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: DOTNET_gcServer
              value: "0"
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "1000m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
