name: ioredis
description: Production-grade Redis client for Dart. Full port of ioredis.
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  crypto: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
